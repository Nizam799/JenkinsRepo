pipeline {
 agent any
 stages {
  stage ('Test') {
  agent { docker { image 'node:16-alpine' } }
   steps {
    sh 'node --version'
    }
   }
  stage ('build') {
   steps {
    sh '''sudo apt install git
    '''
    sh 'git clone "https://github.com/Nizam799/JenkinsRepo.git" /home/snp/'
    }
   }
 }
}
